# WIP — see ../DESIGN.md. This CRD is a placeholder sketch, NOT installed or
# reconciled by anything in this repo. Shipped so the design doc's resource
# has a concrete schema to point at; validation is deliberately minimal.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sentineldeployments.sentinel.datahub.io
spec:
  group: sentinel.datahub.io
  scope: Namespaced
  names:
    plural: sentineldeployments
    singular: sentineldeployment
    kind: SentinelDeployment
    shortNames: [sentinel]
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true # WIP: tighten per DESIGN.md
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
      subresources:
        status: {}
